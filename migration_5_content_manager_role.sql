-- ============================================================
-- МІГРАЦІЯ 5: додаємо роль 'content_manager' в адмін-панель.
-- Запускати ПІСЛЯ попередніх міграцій.
--
-- Колонка users.role вже існує (NVARCHAR(20)) і без CHECK-обмеження,
-- тому схему змінювати не потрібно — достатньо лише створити
-- демо-користувача з цією роллю.
-- ============================================================

-- Демо контент-менеджер (email: manager@cowork.ua, пароль: Manager@2025).
-- Якщо не потрібно — просто не запускайте цей блок.
/*
INSERT INTO [dbo].[users] (full_name, email, password_hash, phone, role)
VALUES (
    N'Контент-менеджер',
    'manager@cowork.ua',
    '$2y$10$ЗАМІНИТИ_НА_РЕАЛЬНИЙ_BCRYPT_ХЕШ',
    '+380XXXXXXXXX',
    'content_manager'
);
*/

-- Якщо хочете перевести існуючого користувача в контент-менеджери:
/*
UPDATE [dbo].[users] SET role = 'content_manager' WHERE email = 'someone@example.com';
*/

PRINT 'Міграція 5: роль content_manager доступна. Демо-юзера додайте вручну розкоментувавши блок вище.';
GO
